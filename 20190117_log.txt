jsp 에서 가장 먼저 읽는 건 스크립틀릿이다.
DB에서 데이터를 빼올 때, mysql 의 자료형과 java 의 자료형이 일치해야 한다.
자바에서는 IO, Network, DB 는 시스템에 큰 영향을 미치므로 try-catch를 강제하는 경우가 종종 있다.
JDBC -> 매번 코드 이렇게 써야되? ... 너무 빡셈... -> 그래서 이 코드들을 하나의 객체에 넣고 그 객체를 갖다 쓰기로함 -> MyBatis !!