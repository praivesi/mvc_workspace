XML : eXtensible MarkUp Language
		(뭐든지 태그로 만들어 쓸 수 있다.)
		
* 특징
1. 시작 태그와 종료 태그가 있어야 한다.
2. 모든 요소명은 대소문자를 "구분" 한다.
3. 요소(엘리먼트, <>) : <요소명>요소내용</요소명>
4. 값이 없다면 빈 요소를 가질 수 있다 : <요소명></요소명>
5. 최상위 엘리먼트 반드시 하나만 가질 수 있음
6. 요소를 겹쳐서 쓰는 것은 허용 x
	ex) <name></name> (o)
		<gender><number></gender></number> (x)
7. 요소명은 xml 이름 지정 규칙을 따라야 함

+ DTD 규칙
 : 회사 내부 및 기업간 전자 상거래 같은 동일한 분야에 관심이 있는 여러 단체(기업)에서
 서로 문서를 주고 받을 때 통일 시킬 목적으로 나름대로 xml 마크업 언어를 개발하여 이 구조를 정의한 것
 
 - 예)
 < 사용 횟수
 + : 1개 이상 ex) 같은 요소가 하나 이상 있어야 한다면 + 기호로 정의
 * : 0개 이상
 ? : 0개 혹은 1개
 
 > 속성 정의 (속성도 dtd 규칙 적용할 수 있음)
 	A|B : A 또는 B 둘 중에 하나만 가능
 	() : 요소를 그룹으로 선언에서 사용
 	ID : 유일한 식별자 -> 중복될 수 있음
 	